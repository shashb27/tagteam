# M0 Critique — approve-with-nits

Re-review after the Boss dispatched fixes for the 6 BLOCKERs raised in the first
pass. Read-only review; only this file was modified. Verification was done by
inspecting the current source on the `opencode` branch (no test re-runs; the
Boss confirms the full suite — lint, typecheck, 88/88 unit, mock E2E, real E2E
551ms on `bigmodel/glm-5.2` — is green).

## Previous BLOCKERs — status

1. **fixed** — `web/app.js:279` pinned AI row now uses `esc(state.modelName)`
   (verified: `ai.innerHTML = '<span class="pt-dot"></span><span class="pt-name">'
   + esc(state.modelName) + '</span><span class="pt-badge">AI</span>'`).
2. **fixed** — `web/app.js:359` composer placeholder reads `'Message the assistant
   — everyone in the session sees this.'`; `642` error fallback is
   `'The assistant hit an error and couldn't respond. Try sending again.'`;
   `786` invite copy is `'Stop playing telephone with your AI.'`;
   `791` gate title is `'Start a shared session'`. No user-facing "Claude"
   remains in `web/app.js`.
3. **fixed** — `server/turns.js:119-130` `friendlyError()` is fully genericized
   to "the assistant" (verified the function body — API key, rate-limit,
   overload, abort, connection, fallback all read "the assistant …").
4. **fixed** — `server/turns.js:139` assistant message is now
   `authorId: 'assistant'`.
5. **fixed** — `server/turns.js:140` fallback is `backend?.assistantName ??
   'Assistant'`; timeout `userMessage` is `'The assistant took too long and was
   stopped.'` (also mirrored at line 221).
6. **fixed** — `server/protocol.js:30` `RATE_LIMITED` now reads `'Too many
   messages queued. Wait for the assistant to catch up.'` The error *code* is
   unchanged; `git diff main -- server/protocol.js` shows only the message-string
   change plus additive JSDoc comments — no structural wire-protocol drift.

## grep audit

`grep -rani "anthropic\|claude" server/ web/` (excluding `node_modules`, `.git`)
returns only:

- `server/config.js:19` + `server/agent/index.js:3,6,13,14` — the
  `MOCK_CLAUDE` env-var identifier (developer-facing test config, non-blocking
  per the rule).
- `server/sessions.js:114,295` — stale comments (NITs, see below).
- `web/style.css:12` — stale CSS comment `/* Claude terracotta */` (NIT).

No user-facing "Claude" string remains in `server/` or `web/`. ✅

## Adapter / config / scope checks

- `server/agent/opencodeRunner.js:134,159` uses
  `process.env.OPENCODE_CWD || process.cwd()` — correct (not
  `DEMO_WORKSPACE_DIR`). ✅
- `package.json` has `@opencode-ai/sdk` and no `@anthropic-ai/*` deps. ✅
- `test/e2e/two-browser-real.spec.js` asserts: non-empty streamed reply, guest
  addressed by name (`toContain('Sam')`), revoke → composer disabled, restore →
  enabled, kick → `#fatal` visible with `/removed from the session/`, reload →
  `#fatal-title` `/already used|single-use|removed/i`. Assertions are real and
  non-trivial. ✅
- No Entra/SSO/Slack/K8s/personas work in M0 (grep finds none). ✅

## New BLOCKERs

None.

## NITs

- `server/sessions.js:114` — comment "(so a name can never spoof the '[Name]:'
  attribution format Claude sees)" → "the agent".
- `server/sessions.js:295` — comment "Abort any in-flight Claude run." →
  "assistant run".
- `web/style.css:12` — comment `/* Claude terracotta */` → drop the brand name.
- `server/protocol.js` — the `git diff` added JSDoc comment blocks on
  `errorFrame` and `sendError`. Not structural, but technically beyond the
  "message text only" scope of blocker 6; harmless and additive.

## Verdict

approve-with-nits — all 6 original BLOCKERs are fixed and verified; remaining
findings are comment-only NITs that do not affect users or the wire protocol.
